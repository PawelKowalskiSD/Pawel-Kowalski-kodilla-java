package com.kodilla.good.patterns.food2Door.logic;

import com.kodilla.good.patterns.food2Door.logic.dataInformation.Producer;

public interface InformationService {
    void producerInformation(Producer producer);
}
